{
  "name": "pc-store-manager-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm:backend\" \"npm:frontend\"",
    "dev:open": "concurrently \"npm:backend\" \"npm:frontend\" \"wait-on http://localhost:3000/health/ready http://localhost:4200 && npx open-cli http://localhost:4200\"",
    "dev:launch": "node start.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "wait-on": "^7.0.1",
    "open-cli": "^8.0.0"
  }
}
